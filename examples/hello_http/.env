# Delivered into the TEE via:
#   tee-crafter deploy ... --secrets-env ./examples/hello_http/.env
PORT=8080
ENVIRONMENT=production
API_TOKEN=demo-rotate-me-in-real-deployments
APP_BANNER=tee-crafter .env injection OK
